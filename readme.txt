1. Install package
npm install
npm i src/did-core/did-core-sdk-1.0.6.tgz

2. Build to runtime folder
npm run build
→ outputs to `dist/`

3. Chrome → `chrome://extensions` → enable **Developer mode** → **Load unpacked** → select the `dist/` folder.

